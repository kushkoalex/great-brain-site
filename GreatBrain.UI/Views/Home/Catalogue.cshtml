@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section scripts
{
    <script type="text/javascript">
        (function (gb, a9) {
            var settings = gb.settings,
                    dataModels = settings.dataModels;
            @Html.Raw(ViewBag.EducationalInstitutions);
            @Html.Raw(ViewBag.EducationalInstitutionFilterGender);
            @Html.Raw(ViewBag.EducationalInstitutionFilterLocation);
            @Html.Raw(ViewBag.EducationalInstitutionFilterType);

        }(GB, A9));
    </script>
}
<div class="page">
    <div id="educationalInstitutions"></div>
</div>